#!/bin/bash
quotes[0]="$HOME/.hitchhikerrc"
quotes[1]="$HOME/.shevekrc"
size=${#quotes[@]}

while true; do
    i=$(($RANDOM % $size))
    echo '' && jq '.[] | .quote' ${quotes[$i]} | shuf -n 39 | head -1 | tr -d '\"' | cowsay -r -C && sleep 92 || exit 1
done
